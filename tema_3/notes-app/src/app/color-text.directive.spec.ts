import { ColorTextDirective } from './color-text.directive';

describe('ColorTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTextDirective();
    expect(directive).toBeTruthy();
  });
});
